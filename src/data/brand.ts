/** Logo en public/ — servido en la raíz del sitio */
export const publicLogoPath = '/logo-ICwhite.svg'

/** Marca oficial del portfolio */
export const brand = {
  alt: 'Studio ICVA — logo IC',
  ogImageAlt: 'Studio ICVA — agencia de productos digitales',
  logo: publicLogoPath,
  logoSvg: publicLogoPath,
  logoPng: publicLogoPath,
  favicon: publicLogoPath,
  appleTouchIcon: publicLogoPath,
} as const
